name: Daily EPIC Earth Image

on:
  schedule:
    - cron: '0 5 * * *'  # Every day at 5 AM UTC
  workflow_dispatch:      # Allows manual triggering from GitHub interface

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Run update script
        run: python update.py

      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add README.md image.jpg
          git commit -m "Update daily Earth image" || echo "No changes to commit"
          git push
